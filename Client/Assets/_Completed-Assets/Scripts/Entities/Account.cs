using KBEngine;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Account : AccountBase
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Account::Start");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
